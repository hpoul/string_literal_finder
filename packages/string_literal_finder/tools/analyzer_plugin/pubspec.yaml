name: string_literal_finder_plugin_loader
description: This pubspec determines the version of the analyzer plugin to load.
version: 1.1.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  string_literal_finder: ^1.1.0+1

